1. 只有两台主变的供区，优化时会出现float division error，因为根据现在的供区容量算法，停一台之后，供区容量就为0，再除以0就会出现float division error。需要在optimization input构建之前，根据以下逻辑先进行供区合环操作：
    1.1 如果供区只有两台主变，则从与当前供区有联络的供区中选择负载率最低的，把所有与目标供区有联络的zone_lines全部合上，合并为一个供区。需要从area_statistics中加载两个供区的配置，然后移除两者互相联络的变电站，合并为一个供区的配置。
    1.2 将合上的zone_lines的操作形成前置操作顺序，作为参数传递到solve_dynamic_recovery_model中，最后return tickets的时候，把前置操作顺序附在generate_tickets返回结果的前面。
2. 在area_topology中，如果线路闸刀初始状态为0，说明是冷备用，不能加到zone_lines里面
{
    "检修设备":"瓯海变电所2号主变",
    "控制设备":"瓯海 3 号或 2 号主变"单元件,
    "送端":"瓯海",
    "稳定限额":80,
    "监控要求及说明":"上田变分列运行方式下瓯海 2 号或 3 号主变停，上田变恢复并列运行"
}